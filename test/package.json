{
  "name": "frida-java-bridge-bundle",
  "version": "1.0.0",
  "description": "Bundled up version of frida-java-bridge for testing",
  "private": true,
  "main": "bundle.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile bundle -o build/frida-java-bridge.js -x -c",
    "watch": "frida-compile bundle -o build/frida-java-bridge.js -x -c -w"
  },
  "license": "LGPL-2.0 WITH WxWindows-exception-3.1",
  "dependencies": {
    "chokidar-cli": "^2.0.0",
    "frida-compile": "^9.0.5"
  }
}

{
  "name": "frida-java-bridge-bundle",
  "version": "1.0.0",
  "description": "Bundled up version of frida-java-bridge for testing",
  "private": true,
  "main": "bundle.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "frida-compile bundle -o build/frida-java-bridge.js -c",
    "watch": "frida-compile bundle -o build/frida-java-bridge.js -w"
  },
  "license": "LGPL-2.0 WITH WxWindows-exception-3.1",
  "dependencies": {
    "chokidar-cli": "^2.1.0",
    "frida-compile": "^10.0.0"
  }
}
